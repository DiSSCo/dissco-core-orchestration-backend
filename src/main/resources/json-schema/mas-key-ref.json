{
  "$id": "https://schemas.dissco.tech/schemas/fdo-type/machine-annotation-service/0.3.0/mas-key-ref.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Machine Annotation Service RequestVersion 0.3.0 - Additional Information",
  "title": "MasRequestKeyReference",
  "type": "object",
  "description": "Key-Value pair whose key is remote. Appropriate for sensitive information",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of an environmental variable stored remotely",
      "examples": [
        "database.password",
        "keycloak.secret"
      ]
    },
    "secretKeyRef": {
      "type": "object",
      "description": "Key-value pair of a remote secret.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the secret store within DiSSCo",
          "examples": [
            "aws-secrets",
            "db-secrets"
          ]
        },
        "key": {
          "type": "string",
          "description": "The name of the key stored in the above secret store. NOT the key's value.",
          "example": [
            "db-password"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "key"
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "name",
    "secretKeyRef"
  ]
}